SELECT IDDEPARTAMENTO, NOMBRE, SUELDO 
FROM (
    SELECT IDDEPARTAMENTO, NOMBRE, SUELDO,
    MIN(SUELDO) OVER (PARTITION BY IDDEPARTAMENTO) MIN_SUELDO  
    FROM RECURSOS.EMPLEADO
) 
WHERE SUELDO = MIN_SUELDO;
